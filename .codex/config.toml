# Axiom Revenue Engine project-local Codex defaults.
# This file contains no credentials and loads only from the trusted non-synced checkout.

model = "gpt-5.6-sol"
model_reasoning_effort = "high"

[agents]
enabled = true
default_subagent_model = "gpt-5.6-luna"
default_subagent_reasoning_effort = "max"
max_concurrent_threads_per_session = 3
